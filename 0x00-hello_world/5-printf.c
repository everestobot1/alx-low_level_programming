#include <stdio.h>

/**
 * main entry point
 * 
 * using printf function to print => with proper grammar, but the outcome is a piece of art, with a newline
 *
 * return 0: (success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}

